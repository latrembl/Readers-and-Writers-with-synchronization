compiled in terminal

to compile (easy):

	gcc rw1.latrembl.c -lpthread		(automatically creates a.out. -lpthread needed for multithreading)
	
to debug (simple):

	gdb a.out			(opens file in gdb)
	r						(runs file. entered in gdb)
